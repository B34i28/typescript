// somethime we don't assign type so typescript give it any type
let student;

student = "Bilal";        // now student have string type
student.toUpperCase()

student = 73;             // now student have number type 
student.toPrecision(1)

// if variable dont have type so we can assign any type to it